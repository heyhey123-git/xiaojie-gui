# Copied into the run directory by the `prepareServerTest` task.
#
# A disposable server whose only player is ever the bot `clientTest` connects with: it exists to boot this
# plugin next to Skript and to read the log afterwards. Everything that only matters for a real server is
# off so startup is short -- including the status ping, which is why the bot is told the protocol instead
# of asking (`server-test/client/bot.mjs`).
online-mode=false
server-port=25598
level-type=minecraft:flat
# The layers are stated because a flat level type without them makes the server log
# "No key layers in MapLike[{}]" at ERROR level while it creates the world, which reads like a
# failure in the run's log.
generator-settings={"layers":[{"block":"minecraft:bedrock","height":1},{"block":"minecraft:dirt","height":2},{"block":"minecraft:grass_block","height":1}],"biome":"minecraft:plains"}
level-name=world
spawn-protection=0
max-players=1
view-distance=2
simulation-distance=2
difficulty=peaceful
enable-command-block=false
enable-status=false
sync-chunk-writes=false
