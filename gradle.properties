# The project's own coordinates. The dependency catalog next to this file lists what the build
# consumes; these describe what the build produces, so the module jars, the shaded jar and plugin.yml
# all report one value instead of repeating a literal in every module.
group=io.github.heyhey123
version=2.0.0

kotlin.code.style=official
shadePrefix=io.github.heyhey123.xiaojiegui.libs
org.gradle.caching=true
